const { changeFile } = require('./changeFile');
const { findAndChangeFiles } = require('./findAndChangeFiles');
const { imageExtensions } = require('./constant');

module.exports = {
    changeFile,
    findAndChangeFiles,
    imageExtensions,
};